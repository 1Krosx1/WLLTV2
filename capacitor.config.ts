import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.MLLT.app',
  appName: 'minasbate-language-learning-app',
  webDir: 'dist'
};

export default config;
